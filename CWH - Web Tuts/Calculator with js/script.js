const calculator = document.querySelector('.calculator');
const key = calculator.querySelector('.calculator__keys');

key.addEventListener('click', e => {
    if (e.target.matches('button')) {

    }
});